# CLAUDE.md

## Night Shift Config

- Test command: composer test
- Build command: composer install --no-dev --no-progress
- Doc language: English
- Push protocol: git push origin <branch>
- Default branch: main

PHP test target: this is a popular PHP logging library (monolog/monolog) forked to perandre/monolog as a Night Shift PHP-support test bed. Tasks that need test/build execution may fail if the routine environment lacks `php` / `composer` — that failure mode is itself a finding.
